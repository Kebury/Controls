<Window x:Class="Controls.Views.AddDepartmentWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Добавить отдел" 
        Height="450" Width="550"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SecondaryBackground}"
        ResizeMode="NoResize">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="Добавление нового отдела" 
                   FontSize="20" 
                   FontWeight="Bold"
                   Foreground="{DynamicResource PrimaryText}"
                   Margin="0,0,0,20"/>

        <Border Grid.Row="1"
                Background="{DynamicResource PrimaryBackground}"
                CornerRadius="8"
                Padding="20"
                Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="Полное наименование отдела*" 
                           FontSize="13" 
                           Foreground="{DynamicResource SecondaryText}"
                           Margin="0,0,0,5"/>
                <TextBox x:Name="FullNameTextBox"
                         FontSize="14"
                         Padding="10"
                         Margin="0,0,0,15"
                         ToolTip="Введите полное наименование отдела"/>

                <TextBlock Text="Укороченное наименование отдела*" 
                           FontSize="13" 
                           Foreground="{DynamicResource SecondaryText}"
                           Margin="0,0,0,5"/>
                <TextBox x:Name="ShortNameTextBox"
                         FontSize="14"
                         Padding="10"
                         Margin="0,0,0,5"
                         ToolTip="Введите краткое наименование отдела для отображения в списке"/>
                
                <TextBlock FontSize="12" 
                           Foreground="{DynamicResource SecondaryText}"
                           FontStyle="Italic"
                           TextWrapping="Wrap"
                           Margin="0,5,0,0"
                           Text="Укороченное наименование будет отображаться в списках, а при наведении курсора будет показано полное наименование"/>
            </StackPanel>
        </Border>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="AddButton"
                    Content="Добавить" 
                    Click="Add_Click"
                    Padding="20,10"
                    Margin="0,0,10,0"
                    Background="{DynamicResource ButtonSuccess}"
                    Foreground="White"
                    FontSize="14"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="5" 
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonSuccessHover}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="Отмена" 
                    Click="Cancel_Click"
                    Padding="20,10"
                    Background="{DynamicResource ButtonSecondary}"
                    Foreground="{DynamicResource PrimaryText}"
                    FontSize="14"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="5" 
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonSecondaryHover}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
