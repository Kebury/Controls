<Window x:Class="Controls.Views.DatabaseLoadingDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подключение к базе данных"
        Width="450"
        Height="320"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None"
        ResizeMode="NoResize"
        Background="Transparent"
        AllowsTransparency="True"
        ShowInTaskbar="True"
        Topmost="True">

    <Border Background="White"
            CornerRadius="12"
            BorderBrush="#007ACC"
            BorderThickness="2"
            Padding="5">
        <Border.Effect>
            <DropShadowEffect Color="Black"
                            Direction="270"
                            ShadowDepth="3"
                            BlurRadius="20"
                            Opacity="0.4"/>
        </Border.Effect>

        <Grid Margin="30,25,30,25">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,25">
                <TextBlock Text="⏳" 
                         FontSize="24"
                         Margin="0,0,10,0"
                         VerticalAlignment="Center"
                         RenderTransformOrigin="0.5,0.5">
                    <TextBlock.RenderTransform>
                        <RotateTransform x:Name="HourglassRotation" Angle="0"/>
                    </TextBlock.RenderTransform>
                    <TextBlock.Triggers>
                        <EventTrigger RoutedEvent="Loaded">
                            <BeginStoryboard>
                                <Storyboard RepeatBehavior="Forever">
                                    <DoubleAnimation Storyboard.TargetName="HourglassRotation"
                                                   Storyboard.TargetProperty="Angle"
                                                   From="0" To="360" Duration="0:0:2"/>
                                </Storyboard>
                            </BeginStoryboard>
                        </EventTrigger>
                    </TextBlock.Triggers>
                </TextBlock>
                <TextBlock Text="Ожидание подключения к базе данных"
                         FontSize="17"
                         FontWeight="SemiBold"
                         VerticalAlignment="Center"
                         Foreground="#333333"/>
            </StackPanel>

            <StackPanel Grid.Row="1"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center">
                
                <Grid Width="110" Height="110" Margin="0,0,0,20">
                    <Ellipse Width="90"
                           Height="90"
                           Stroke="#E8E8E8"
                           StrokeThickness="7"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"/>
                    
                    <Ellipse x:Name="LoadingEllipse"
                           Width="90"
                           Height="90"
                           Stroke="#007ACC"
                           StrokeThickness="7"
                           StrokeDashArray="25,10"
                           StrokeStartLineCap="Round"
                           StrokeEndLineCap="Round"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           RenderTransformOrigin="0.5,0.5">
                        <Ellipse.RenderTransform>
                            <RotateTransform x:Name="LoadingRotation"/>
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="LoadingRotation"
                                                       Storyboard.TargetProperty="Angle"
                                                       From="0"
                                                       To="360"
                                                       Duration="0:0:1.4"
                                                       RepeatBehavior="Forever"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <TextBlock x:Name="StatusTextBlock"
                             Text="Подключение"
                             FontSize="14"
                             Foreground="#666666"/>
                    <TextBlock x:Name="AnimatedDots"
                             Text="..."
                             FontSize="14"
                             Foreground="#666666">
                        <TextBlock.Triggers>
                            <EventTrigger RoutedEvent="Loaded">
                                <BeginStoryboard>
                                    <Storyboard RepeatBehavior="Forever">
                                        <StringAnimationUsingKeyFrames Storyboard.TargetProperty="Text"
                                                                      Duration="0:0:1.5">
                                            <DiscreteStringKeyFrame KeyTime="0:0:0" Value=""/>
                                            <DiscreteStringKeyFrame KeyTime="0:0:0.5" Value="."/>
                                            <DiscreteStringKeyFrame KeyTime="0:0:1" Value=".."/>
                                            <DiscreteStringKeyFrame KeyTime="0:0:1.5" Value="..."/>
                                        </StringAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </TextBlock.Triggers>
                    </TextBlock>
                </StackPanel>
            </StackPanel>

            <Button Grid.Row="2"
                   Content="✕ Отменить"
                   Click="Cancel_Click"
                   Padding="30,10"
                   HorizontalAlignment="Center"
                   Background="#F44336"
                   Foreground="White"
                   BorderThickness="0"
                   Cursor="Hand"
                   FontSize="14"
                   FontWeight="Medium">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border"
                               Background="{TemplateBinding Background}"
                               CornerRadius="6"
                               Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D32F2F"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B71C1C"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>
