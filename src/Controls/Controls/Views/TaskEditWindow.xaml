<Window x:Class="Controls.Views.TaskEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:models="clr-namespace:Controls.Models"
        mc:Ignorable="d"
        Title="{Binding WindowTitle}" 
        Height="700" Width="900"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SecondaryBackground}"
        FocusManager.FocusedElement="{Binding ElementName=MainGrid}">
    
    <Grid x:Name="MainGrid" 
          Margin="30"
          Focusable="True">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="{Binding WindowTitle}" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Foreground="{DynamicResource PrimaryText}"
                   Margin="0,0,0,25"/>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Border Background="{DynamicResource PrimaryBackground}" 
                        BorderBrush="{DynamicResource PrimaryBorder}" 
                        BorderThickness="1"
                        CornerRadius="8" 
                        Padding="20"
                        Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📋 Основные данные" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,15"/>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Наименование*" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Title, UpdateSourceTrigger=PropertyChanged}" 
                                     IsEnabled="{Binding CanEditFields}"
                                     FontSize="14"
                                     Padding="10"
                                     BorderBrush="{DynamicResource PrimaryBorder}"
                                     BorderThickness="1"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Содержание" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" 
                                     IsEnabled="{Binding CanEditFields}"
                                     FontSize="14"
                                     Padding="10"
                                     Height="100"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     BorderBrush="{DynamicResource PrimaryBorder}"
                                     BorderThickness="1"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Тип задания*" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding TaskTypes}"
                                      IsEnabled="{Binding CanEditFields}"
                                      SelectedItem="{Binding SelectedTaskType}"
                                      FontSize="14"
                                      Padding="10"
                                      BorderBrush="{DynamicResource PrimaryBorder}"
                                      BorderThickness="1"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Исполнитель (полное ФИО)" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding Assignees}"
                                      IsEnabled="{Binding CanEditFields}"
                                      Text="{Binding Assignee}"
                                      IsEditable="True"
                                      FontSize="14"
                                      Padding="10"
                                      BorderBrush="{DynamicResource PrimaryBorder}"
                                      BorderThickness="1"
                                      ToolTip="Введите полное ФИО исполнителя (например: Иванов Иван Иванович)">
                                <ComboBox.ItemContainerStyle>
                                    <Style TargetType="ComboBoxItem" BasedOn="{StaticResource {x:Type ComboBoxItem}}">
                                        <Setter Property="Padding" Value="10,5"/>
                                    </Style>
                                </ComboBox.ItemContainerStyle>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource PrimaryBackground}" 
                        BorderBrush="{DynamicResource PrimaryBorder}" 
                        BorderThickness="1"
                        CornerRadius="8" 
                        Padding="20"
                        Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="⚠️ Приоритет" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,15"/>

                        <StackPanel>
                            <TextBlock Text="Важность*" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <ComboBox ItemsSource="{Binding ImportancePriorities}"
                                      IsEnabled="{Binding CanEditFields}"
                                      SelectedItem="{Binding SelectedImportance}"
                                      FontSize="14"
                                      Padding="10"
                                      BorderBrush="{DynamicResource PrimaryBorder}"
                                      BorderThickness="1">
                                <ComboBox.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding}"/>
                                    </DataTemplate>
                                </ComboBox.ItemTemplate>
                            </ComboBox>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource PrimaryBackground}" 
                        BorderBrush="{DynamicResource PrimaryBorder}" 
                        BorderThickness="1"
                        CornerRadius="8" 
                        Padding="20"
                        Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📅 Сроки" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,15"/>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Дата поступления*" 
                                           FontSize="13" 
                                           Foreground="{DynamicResource SecondaryText}"
                                           Margin="0,0,0,5"/>
                                <DatePicker SelectedDate="{Binding CreatedDate}"
                                            IsEnabled="{Binding CanEditFields}"
                                            FontSize="14"
                                            Padding="10"
                                            BorderBrush="{DynamicResource PrimaryBorder}"
                                            BorderThickness="1"
                                            Cursor="Hand"/>
                            </StackPanel>

                            <StackPanel Grid.Column="2">
                                <TextBlock Text="Срок исполнения*" 
                                           FontSize="13" 
                                           Foreground="{DynamicResource SecondaryText}"
                                           Margin="0,0,0,5"/>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <DatePicker Grid.Column="0"
                                                SelectedDate="{Binding DueDate}"
                                                IsEnabled="{Binding CanEditFields}"
                                                FontSize="14"
                                                Padding="10"
                                                BorderBrush="{DynamicResource PrimaryBorder}"
                                                BorderThickness="1"
                                                Cursor="Hand"
                                                Margin="0,0,10,0"/>

                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                        <TextBox Text="{Binding DueTimeHour, UpdateSourceTrigger=PropertyChanged}"
                                                 IsEnabled="{Binding CanEditFields}"
                                                 Width="60"
                                                 FontSize="14"
                                                 Padding="8"
                                                 BorderBrush="{DynamicResource PrimaryBorder}"
                                                 BorderThickness="1"
                                                 Margin="0,0,5,0"
                                                 VerticalContentAlignment="Center"
                                                 HorizontalContentAlignment="Center"
                                                 ToolTip="Час (0-23)"
                                                 MaxLength="2"
                                                 PreviewTextInput="NumericTextBox_PreviewTextInput"/>
                                        <TextBlock Text=":" 
                                                   VerticalAlignment="Center" 
                                                   FontSize="16" 
                                                   FontWeight="Bold"
                                                   Margin="0,0,5,0"/>
                                        <TextBox Text="{Binding DueTimeMinute, UpdateSourceTrigger=PropertyChanged}"
                                                 IsEnabled="{Binding CanEditFields}"
                                                 Width="60"
                                                 FontSize="14"
                                                 Padding="8"
                                                 BorderBrush="{DynamicResource PrimaryBorder}"
                                                 BorderThickness="1"
                                                 VerticalContentAlignment="Center"
                                                 HorizontalContentAlignment="Center"
                                                 ToolTip="Минуты (0-59)"
                                                 MaxLength="2"
                                                 PreviewTextInput="NumericTextBox_PreviewTextInput"/>
                                    </StackPanel>
                                </Grid>
                            </StackPanel>
                        </Grid>
                        
                        <TextBlock Text="Срок рассчитывается на основе типа задания и даты поступления" 
                                   FontSize="11" 
                                   Foreground="{DynamicResource TertiaryText}"
                                   Margin="0,10,0,0"
                                   FontStyle="Italic"/>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource PrimaryBackground}" 
                        BorderBrush="{DynamicResource PrimaryBorder}" 
                        BorderThickness="1"
                        CornerRadius="8" 
                        Padding="20"
                        Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="💡 Дополнительная информация" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,15"/>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Номер задания" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding ControlNumber, UpdateSourceTrigger=PropertyChanged}" 
                                     IsEnabled="{Binding CanEditFields}"
                                     FontSize="14"
                                     Padding="10"
                                     BorderBrush="{DynamicResource PrimaryBorder}"
                                     BorderThickness="1"/>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Основной документ (путь к документу)" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"
                                         Text="{Binding ControlDocumentPath, UpdateSourceTrigger=PropertyChanged}" 
                                         FontSize="14"
                                         Padding="10"
                                         BorderBrush="{DynamicResource PrimaryBorder}"
                                         BorderThickness="1"
                                         Margin="0,0,10,0"/>
                                <Button Grid.Column="1"
                                        Content="📁"
                                        Command="{Binding BrowseControlDocumentCommand}"
                                        FontSize="14"
                                        Padding="15,10"
                                        Background="{DynamicResource ButtonPrimary}"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        ToolTip="Обзор..."/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Шаблон ответа (путь к файлу)" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"
                                         Text="{Binding ReportTemplatePath, UpdateSourceTrigger=PropertyChanged}" 
                                         FontSize="14"
                                         Padding="10"
                                         BorderBrush="{DynamicResource PrimaryBorder}"
                                         BorderThickness="1"
                                         Margin="0,0,10,0"/>
                                <Button Grid.Column="1"
                                        Content="📁"
                                        Command="{Binding BrowseReportTemplateCommand}"
                                        FontSize="14"
                                        Padding="15,10"
                                        Background="{DynamicResource ButtonPrimary}"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        ToolTip="Обзор..."/>
                            </Grid>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,15">
                            <TextBlock Text="Папка с донесениями (путь к сетевой папке)" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBox Grid.Column="0"
                                         Text="{Binding ReportsFolderPath, UpdateSourceTrigger=PropertyChanged}" 
                                         FontSize="14"
                                         Padding="10"
                                         BorderBrush="{DynamicResource PrimaryBorder}"
                                         BorderThickness="1"
                                         Margin="0,0,10,0"/>
                                <Button Grid.Column="1"
                                        Content="📁"
                                        Command="{Binding BrowseReportsFolderCommand}"
                                        FontSize="14"
                                        Padding="15,10"
                                        Background="{DynamicResource ButtonPrimary}"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand"
                                        ToolTip="Обзор..."/>
                            </Grid>
                        </StackPanel>

                        <StackPanel>
                            <TextBlock Text="Примечания" 
                                       FontSize="13" 
                                       Foreground="{DynamicResource SecondaryText}"
                                       Margin="0,0,0,5"/>
                            <TextBox Text="{Binding Notes, UpdateSourceTrigger=PropertyChanged}" 
                                     IsEnabled="{Binding CanEditFields}"
                                     FontSize="14"
                                     Padding="10"
                                     Height="80"
                                     TextWrapping="Wrap"
                                     AcceptsReturn="True"
                                     VerticalScrollBarVisibility="Auto"
                                     BorderBrush="{DynamicResource PrimaryBorder}"
                                     BorderThickness="1"/>
                        </StackPanel>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource PrimaryBackground}" 
                        BorderBrush="{DynamicResource PrimaryBorder}" 
                        BorderThickness="1"
                        CornerRadius="8" 
                        Padding="20"
                        Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📎 Прикреплённые документы" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,20"/>

                        <StackPanel Margin="0,0,0,20">
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="📋 Основной документ" 
                                           FontSize="14" 
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryText}"
                                           VerticalAlignment="Center"/>
                                <Button Grid.Column="1"
                                        Content="➕ Добавить"
                                        Command="{Binding AttachControlDocumentCommand}"
                                        FontSize="12"
                                        Padding="10,5"
                                        Background="{DynamicResource ButtonPrimary}"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="{DynamicResource ButtonPrimary}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="4"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                              VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource ButtonPrimaryHover}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>

                            <ItemsControl ItemsSource="{Binding ControlDocuments}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource SecondaryBackground}"
                                                BorderBrush="{DynamicResource PrimaryBorder}"
                                                BorderThickness="1"
                                                CornerRadius="5"
                                                Padding="10"
                                                Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="📄"
                                                           FontSize="16"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,10,0"/>

                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding FileName}" 
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="{DynamicResource PrimaryText}"/>
                                                    <TextBlock Text="{Binding AddedDate, StringFormat='Добавлено: {0:dd.MM.yyyy HH:mm}'}" 
                                                               FontSize="11"
                                                               Foreground="{DynamicResource SecondaryText}"/>
                                                </StackPanel>

                                                <Button Grid.Column="2"
                                                        Content="👁️"
                                                        Command="{Binding DataContext.OpenDocumentCommand, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}"
                                                        CommandParameter="{Binding}"
                                                        FontSize="14"
                                                        Padding="8,4"
                                                        Margin="5,0"
                                                        Background="#27AE60"
                                                        Foreground="White"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        ToolTip="Открыть">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="{DynamicResource ButtonSuccess}"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}" 
                                                                                CornerRadius="3"
                                                                                Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                                              VerticalAlignment="Center"/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="{DynamicResource ButtonSuccessHover}"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>

                                                <Button Grid.Column="3"
                                                        Content="🗑️"
                                                        Command="{Binding DataContext.RemoveDocumentCommand, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}"
                                                        CommandParameter="{Binding}"
                                                        FontSize="14"
                                                        Padding="8,4"
                                                        Background="#E74C3C"
                                                        Foreground="White"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        ToolTip="Удалить">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="{DynamicResource ButtonDanger}"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}" 
                                                                                CornerRadius="3"
                                                                                Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                                              VerticalAlignment="Center"/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="{DynamicResource ButtonDangerHover}"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock Text="Документы не прикреплены"
                                       FontSize="12"
                                       Foreground="{DynamicResource TertiaryText}"
                                       FontStyle="Italic"
                                       Margin="0,5,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ControlDocuments.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel Margin="0,0,0,20">
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="📝 Шаблон ответа" 
                                           FontSize="14" 
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryText}"
                                           VerticalAlignment="Center"/>
                                <Button Grid.Column="1"
                                        Content="➕ Добавить"
                                        Command="{Binding AttachReportTemplateDocumentCommand}"
                                        FontSize="12"
                                        Padding="10,5"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="{DynamicResource ButtonPrimary}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="4"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                              VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource ButtonPrimaryHover}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>

                            <ItemsControl ItemsSource="{Binding ReportTemplateDocuments}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource TertiaryBackground}"
                                                BorderBrush="{DynamicResource PrimaryBorder}"
                                                BorderThickness="1"
                                                CornerRadius="5"
                                                Padding="10"
                                                Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="📄"
                                                           FontSize="16"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,10,0"/>

                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding FileName}" 
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="{DynamicResource PrimaryText}"/>
                                                    <TextBlock Text="{Binding AddedDate, StringFormat='Добавлено: {0:dd.MM.yyyy HH:mm}'}" 
                                                               FontSize="11"
                                                               Foreground="{DynamicResource SecondaryText}"/>
                                                </StackPanel>

                                                <Button Grid.Column="2"
                                                        Content="👁️"
                                                        Command="{Binding DataContext.OpenDocumentCommand, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}"
                                                        CommandParameter="{Binding}"
                                                        FontSize="14"
                                                        Padding="8,4"
                                                        Margin="5,0"
                                                        Background="#27AE60"
                                                        Foreground="White"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        ToolTip="Открыть">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="{DynamicResource ButtonSuccess}"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}" 
                                                                                CornerRadius="3"
                                                                                Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                                              VerticalAlignment="Center"/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="{DynamicResource ButtonSuccessHover}"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>

                                                <Button Grid.Column="3"
                                                        Content="🗑️"
                                                        Command="{Binding DataContext.RemoveDocumentCommand, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}"
                                                        CommandParameter="{Binding}"
                                                        FontSize="14"
                                                        Padding="8,4"
                                                        Background="#E74C3C"
                                                        Foreground="White"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        ToolTip="Удалить">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="{DynamicResource ButtonDanger}"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}" 
                                                                                CornerRadius="3"
                                                                                Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                                              VerticalAlignment="Center"/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="{DynamicResource ButtonDangerHover}"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock Text="Документы не прикреплены"
                                       FontSize="12"
                                       Foreground="{DynamicResource TertiaryText}"
                                       FontStyle="Italic"
                                       Margin="0,5,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ReportTemplateDocuments.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>

                        <StackPanel>
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0"
                                           Text="📤 Направленные ответы" 
                                           FontSize="14" 
                                           FontWeight="SemiBold"
                                           Foreground="{DynamicResource PrimaryText}"
                                           VerticalAlignment="Center"/>
                                <Button Grid.Column="1"
                                        Content="➕ Добавить"
                                        Command="{Binding AttachSentReportDocumentCommand}"
                                        FontSize="12"
                                        Padding="10,5"
                                        Foreground="White"
                                        BorderThickness="0"
                                        Cursor="Hand">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Setter Property="Background" Value="{DynamicResource ButtonPrimary}"/>
                                            <Setter Property="Template">
                                                <Setter.Value>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" 
                                                                CornerRadius="4"
                                                                Padding="{TemplateBinding Padding}">
                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                              VerticalAlignment="Center"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Setter.Value>
                                            </Setter>
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource ButtonPrimaryHover}"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </Grid>

                            <ItemsControl ItemsSource="{Binding SentReportsDocuments}">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Background="{DynamicResource TertiaryBackground}"
                                                BorderBrush="{DynamicResource PrimaryBorder}"
                                                BorderThickness="1"
                                                CornerRadius="5"
                                                Padding="10"
                                                Margin="0,0,0,8">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Grid.Column="0"
                                                           Text="📄"
                                                           FontSize="16"
                                                           VerticalAlignment="Center"
                                                           Margin="0,0,10,0"/>

                                                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                    <TextBlock Text="{Binding FileName}" 
                                                               FontSize="13"
                                                               FontWeight="SemiBold"
                                                               Foreground="{DynamicResource PrimaryText}"/>
                                                    <TextBlock Text="{Binding AddedDate, StringFormat='Добавлено: {0:dd.MM.yyyy HH:mm}'}" 
                                                               FontSize="11"
                                                               Foreground="{DynamicResource SecondaryText}"/>
                                                </StackPanel>

                                                <Button Grid.Column="2"
                                                        Content="👁️"
                                                        Command="{Binding DataContext.OpenDocumentCommand, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}"
                                                        CommandParameter="{Binding}"
                                                        FontSize="14"
                                                        Padding="8,4"
                                                        Margin="5,0"
                                                        Background="#27AE60"
                                                        Foreground="White"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        ToolTip="Открыть">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="{DynamicResource ButtonSuccess}"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}" 
                                                                                CornerRadius="3"
                                                                                Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                                              VerticalAlignment="Center"/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="{DynamicResource ButtonSuccessHover}"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>

                                                <Button Grid.Column="3"
                                                        Content="🗑️"
                                                        Command="{Binding DataContext.RemoveDocumentCommand, RelativeSource={RelativeSource AncestorType=Border, AncestorLevel=2}}"
                                                        CommandParameter="{Binding}"
                                                        FontSize="14"
                                                        Padding="8,4"
                                                        Background="#E74C3C"
                                                        Foreground="White"
                                                        BorderThickness="0"
                                                        Cursor="Hand"
                                                        ToolTip="Удалить">
                                                    <Button.Style>
                                                        <Style TargetType="Button">
                                                            <Setter Property="Background" Value="{DynamicResource ButtonDanger}"/>
                                                            <Setter Property="Template">
                                                                <Setter.Value>
                                                                    <ControlTemplate TargetType="Button">
                                                                        <Border Background="{TemplateBinding Background}" 
                                                                                CornerRadius="3"
                                                                                Padding="{TemplateBinding Padding}">
                                                                            <ContentPresenter HorizontalAlignment="Center" 
                                                                                              VerticalAlignment="Center"/>
                                                                        </Border>
                                                                    </ControlTemplate>
                                                                </Setter.Value>
                                                            </Setter>
                                                            <Style.Triggers>
                                                                <Trigger Property="IsMouseOver" Value="True">
                                                                    <Setter Property="Background" Value="{DynamicResource ButtonDangerHover}"/>
                                                                </Trigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Button.Style>
                                                </Button>
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>

                            <TextBlock Text="Документы не прикреплены"
                                       FontSize="12"
                                       Foreground="{DynamicResource TertiaryText}"
                                       FontStyle="Italic"
                                       Margin="0,5,0,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding SentReportsDocuments.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="2" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Отмена"
                    Command="{Binding CancelCommand}"
                    FontSize="14"
                    Padding="20,10"
                    Margin="0,0,10,0"
                    Background="{DynamicResource ButtonSecondary}"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{DynamicResource ButtonSecondary}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="5"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonSecondaryHover}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>

            <Button Content="Сохранить"
                    Command="{Binding SaveCommand}"
                    Visibility="{Binding CanEditFields, Converter={StaticResource BoolToVisibilityConverter}}"
                    FontSize="14"
                    Padding="20,10"
                    Background="{DynamicResource ButtonSuccess}"
                    Foreground="White"
                    BorderThickness="0"
                    Cursor="Hand">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="{DynamicResource ButtonSuccess}"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                            CornerRadius="5"
                                            Padding="{TemplateBinding Padding}">
                                        <ContentPresenter HorizontalAlignment="Center" 
                                                          VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonSuccessHover}"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource PrimaryBorder}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
