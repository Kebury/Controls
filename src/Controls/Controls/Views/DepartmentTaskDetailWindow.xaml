<Window x:Class="Controls.Views.DepartmentTaskDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:helpers="clr-namespace:Controls.Helpers"
        xmlns:converters="clr-namespace:Controls"
        mc:Ignorable="d"
        Title="Детали задания" 
        Height="700" Width="800"
        MinHeight="560" MinWidth="640"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SecondaryBackground}"
        ResizeMode="CanResizeWithGrip"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <helpers:IsNotNullOrEmptyConverter x:Key="IsNotNullOrEmptyConverter"/>
        <helpers:InverseCountToVisibilityConverter x:Key="InverseCountToVisibilityConverter"/>
    </Window.Resources>
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="{Binding TaskNumber}" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="{DynamicResource PrimaryText}"
                       TextWrapping="Wrap"/>
            
            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Border Background="{DynamicResource ButtonPrimary}" 
                        CornerRadius="3" 
                        Padding="10,4"
                        Margin="0,0,10,0">
                    <TextBlock Text="{Binding Department}" 
                               Foreground="White" 
                               FontSize="12"
                               FontWeight="SemiBold"/>
                </Border>

                <Border Background="{DynamicResource ButtonSuccess}" 
                        CornerRadius="3" 
                        Padding="10,4"
                        Margin="0,0,10,0">
                    <Border.Style>
                        <Style TargetType="Border">
                            <Setter Property="Background" Value="{DynamicResource ButtonSuccess}"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsCompleted}" Value="False">
                                    <Setter Property="Background" Value="#FF9800"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <TextBlock Text="{Binding StatusText}"
                               Foreground="White" 
                               FontSize="12"
                               FontWeight="SemiBold"/>
                </Border>
                
                <Border Background="{DynamicResource ButtonDanger}" 
                        CornerRadius="3" 
                        Padding="10,4"
                        Margin="0,0,10,0">
                    <TextBlock Text="{Binding ImportanceDisplay}" 
                               Foreground="White" 
                               FontSize="12"
                               FontWeight="SemiBold"/>
                </Border>
            </StackPanel>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <TabControl>
                <TabItem Header="📋 Основная информация" FontSize="14" FontWeight="SemiBold">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                        <StackPanel>
                            <Border Background="{DynamicResource PrimaryBackground}" 
                                    BorderBrush="{DynamicResource PrimaryBorder}" 
                                    BorderThickness="1"
                                    CornerRadius="8" 
                                    Padding="20"
                                    Margin="0,0,0,15">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="Номер задания:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,15,12"/>
                        <TextBlock Grid.Row="0" Grid.Column="1"
                                   Text="{Binding TaskNumber}"
                                   FontSize="14"
                                   FontWeight="Bold"
                                   Foreground="{DynamicResource ButtonPrimary}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,12"/>

                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Text="Организация:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,15,12"/>
                        <TextBlock Grid.Row="1" Grid.Column="1"
                                   Text="{Binding Department}"
                                   FontSize="14"
                                   Foreground="{DynamicResource PrimaryText}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,12"/>

                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="Направлено в организацию:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,15,12"/>
                        <TextBlock Grid.Row="2" Grid.Column="1"
                                   Text="{Binding Department}"
                                   FontSize="14"
                                   Foreground="{DynamicResource PrimaryText}"
                                   TextWrapping="Wrap"
                                   Margin="0,0,0,12"/>

                        <TextBlock Grid.Row="3" Grid.Column="0"
                                   Text="Дата направления:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,15,12"/>
                        <TextBlock Grid.Row="3" Grid.Column="1"
                                   Text="{Binding SentDate, StringFormat='dd.MM.yyyy'}"
                                   FontSize="14"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,12"/>

                        <TextBlock Grid.Row="4" Grid.Column="0"
                                   Text="Дата исполнения:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,15,12"/>
                        <StackPanel Grid.Row="4" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="{Binding DueDate, StringFormat='dd.MM.yyyy'}"
                                       FontSize="14"
                                       Foreground="{DynamicResource PrimaryText}"
                                       Margin="0,0,10,0"/>
                            <TextBlock FontSize="13">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding DaysRemaining, StringFormat='({0} дн.)'}"/>
                                        <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsOverdue}" Value="True">
                                                <Setter Property="Text" Value="(просрочено)"/>
                                                <Setter Property="Foreground" Value="#F44336"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </StackPanel>

                        <TextBlock Grid.Row="5" Grid.Column="0"
                                   Text="Важность:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,15,12"/>
                        <TextBlock Grid.Row="5" Grid.Column="1"
                                   Text="{Binding ImportanceDisplay}"
                                   FontSize="14"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,12"/>

                        <TextBlock Grid.Row="6" Grid.Column="0"
                                   Text="Исполнитель:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,15,12"/>
                        <TextBlock Grid.Row="6" Grid.Column="1"
                                   Text="{Binding ExecutorShortName}"
                                   FontSize="14"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,12"
                                   ToolTip="{Binding ExecutorFullInfo}"/>

                        <TextBlock Grid.Row="7" Grid.Column="0"
                                   Text="Исполнено:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryText}"
                                   Margin="0,0,15,12"/>
                        <StackPanel Grid.Row="7" Grid.Column="1" Orientation="Horizontal" Margin="0,0,0,12">
                            <TextBlock Text="{Binding IsCompleted, StringFormat='{}{0:Да;Нет;Нет}'}"
                                       FontSize="14"
                                       Foreground="{DynamicResource PrimaryText}"/>
                            <TextBlock Text="{Binding CompletedDate, StringFormat=' (дата: {0:dd.MM.yyyy})'}"
                                       FontSize="14"
                                       Foreground="{DynamicResource SecondaryText}"
                                       Visibility="{Binding IsCompleted, Converter={StaticResource BoolToVisibilityConverter}}"/>
                        </StackPanel>

                        <TextBlock Grid.Row="8" Grid.Column="0"
                                   Text="Описание:"
                                   FontSize="13"
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource SecondaryText}"
                                   VerticalAlignment="Top"
                                   Margin="0,0,15,0"/>
                        <TextBlock Grid.Row="8" Grid.Column="1"
                                   Text="{Binding Description}"
                                   FontSize="14"
                                   Foreground="{DynamicResource PrimaryText}"
                                   TextWrapping="Wrap"/>
                    </Grid>
                </Border>
                            <Border Background="{DynamicResource PrimaryBackground}"
                                    BorderBrush="{DynamicResource PrimaryBorder}"
                                    BorderThickness="1"
                                    CornerRadius="8"
                                    Padding="20"
                                    Margin="0,0,0,15"
                                    Visibility="{Binding HasMultipleDepartments, Converter={StaticResource BoolToVisibilityConverter}}">
                                <StackPanel>
                                    <TextBlock Text="📋 Статус исполнения по организациям"
                                               FontSize="16"
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource PrimaryText}"
                                               Margin="0,0,0,15"/>
                                    
                                    <ItemsControl ItemsSource="{Binding DepartmentStatuses}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border Background="{DynamicResource SecondaryBackground}"
                                                        BorderBrush="{DynamicResource PrimaryBorder}"
                                                        BorderThickness="1"
                                                        CornerRadius="5"
                                                        Padding="15,10"
                                                        Margin="0,0,0,10">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        
                                                        <StackPanel Grid.Column="0">
                                                            <TextBlock Text="{Binding DepartmentName}"
                                                                       FontSize="14"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="{DynamicResource PrimaryText}"
                                                                       Margin="0,0,0,5"/>
                                                            <TextBlock Text="{Binding DepartmentFullName}"
                                                                       FontSize="12"
                                                                       Foreground="{DynamicResource SecondaryText}"
                                                                       TextWrapping="Wrap"/>
                                                        </StackPanel>
                                                        
                                                        <Border Grid.Column="1"
                                                                Background="{Binding StatusColor}"
                                                                CornerRadius="5"
                                                                Padding="12,6"
                                                                Margin="10,0,0,0"
                                                                VerticalAlignment="Center">
                                                            <TextBlock Text="{Binding StatusText}"
                                                                       FontSize="12"
                                                                       FontWeight="SemiBold"
                                                                       Foreground="White"
                                                                       TextWrapping="NoWrap"/>
                                                        </Border>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                    
                                    <TextBlock Text="Нет информации об организациях"
                                               FontSize="13"
                                               FontStyle="Italic"
                                               Foreground="{DynamicResource TertiaryText}"
                                               Margin="0,5,0,0"
                                               Visibility="{Binding DepartmentStatuses.Count, Converter={StaticResource InverseCountToVisibilityConverter}}"/>
                                </StackPanel>
                            </Border>

                <Border Background="{DynamicResource PrimaryBackground}" 
                        BorderBrush="{DynamicResource PrimaryBorder}" 
                        BorderThickness="1"
                        CornerRadius="8" 
                        Padding="20"
                        Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📎 Прилагаемые файлы задания" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,15"/>

                        <ItemsControl ItemsSource="{Binding TaskFiles}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.OpenFileCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Left"
                                            Margin="0,0,0,8"
                                            Padding="10,6"
                                            FontSize="13"
                                            BorderThickness="0"
                                            Cursor="Hand">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📄" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis" MaxWidth="600"/>
                                        </StackPanel>
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Foreground" Value="{DynamicResource ButtonPrimary}"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    CornerRadius="4"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource HoverBackground}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="Нет прилагаемых файлов"
                                   FontSize="13"
                                   FontStyle="Italic"
                                   Foreground="{DynamicResource TertiaryText}"
                                   Visibility="{Binding TaskFiles.Count, Converter={StaticResource InverseCountToVisibilityConverter}}"/>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource PrimaryBackground}" 
                        BorderBrush="{DynamicResource PrimaryBorder}" 
                        BorderThickness="1"
                        CornerRadius="8" 
                        Padding="20"
                        Margin="0,0,0,15">
                    <StackPanel>
                        <TextBlock Text="📨 Файлы, поступающие от организаций" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,15"/>

                        <ItemsControl ItemsSource="{Binding DepartmentFiles}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.OpenFileCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                            CommandParameter="{Binding}"
                                            HorizontalAlignment="Left"
                                            Margin="0,0,0,8"
                                            Padding="10,6"
                                            FontSize="13"
                                            BorderThickness="0"
                                            Cursor="Hand">
                                        <StackPanel Orientation="Horizontal">
                                            <TextBlock Text="📄" Margin="0,0,8,0"/>
                                            <TextBlock Text="{Binding}" TextTrimming="CharacterEllipsis" MaxWidth="600"/>
                                        </StackPanel>
                                        <Button.Style>
                                            <Style TargetType="Button">
                                                <Setter Property="Background" Value="Transparent"/>
                                                <Setter Property="Foreground" Value="{DynamicResource ButtonPrimary}"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="Button">
                                                            <Border Background="{TemplateBinding Background}"
                                                                    CornerRadius="4"
                                                                    Padding="{TemplateBinding Padding}">
                                                                <ContentPresenter HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                                            </Border>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                                <Style.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Background" Value="{DynamicResource HoverBackground}"/>
                                                    </Trigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <TextBlock Text="Нет файлов от организаций"
                                   FontSize="13"
                                   FontStyle="Italic"
                                   Foreground="{DynamicResource TertiaryText}"
                                   Visibility="{Binding DepartmentFiles.Count, Converter={StaticResource InverseCountToVisibilityConverter}}"/>
                    </StackPanel>
                </Border>

                <Border Background="{DynamicResource PrimaryBackground}" 
                        BorderBrush="{DynamicResource PrimaryBorder}" 
                        BorderThickness="1"
                        CornerRadius="8" 
                        Padding="20"
                        Visibility="{Binding Notes, Converter={StaticResource IsNotNullOrEmptyConverter}}">
                    <StackPanel>
                        <TextBlock Text="📝 Примечания" 
                                   FontSize="16" 
                                   FontWeight="SemiBold"
                                   Foreground="{DynamicResource PrimaryText}"
                                   Margin="0,0,0,10"/>
                        <TextBlock Text="{Binding Notes}"
                                   FontSize="14"
                                   Foreground="{DynamicResource PrimaryText}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>

                <TabItem Header="📜 История" FontSize="14" FontWeight="SemiBold">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0,10,0,0">
                        <StackPanel>
                            <TextBlock Text="История выполнения задания" 
                                       FontSize="18" 
                                       FontWeight="SemiBold"
                                       Foreground="{DynamicResource PrimaryText}"
                                       Margin="0,0,0,15"/>
                            
                            <Border Background="{DynamicResource PrimaryBackground}" 
                                    BorderBrush="{DynamicResource PrimaryBorder}" 
                                    BorderThickness="1"
                                    CornerRadius="8" 
                                    Padding="20">
                                <StackPanel>
                                    <TextBlock FontSize="14" 
                                               Foreground="{DynamicResource SecondaryText}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,15">
                                        <Run Text="Задание направлено:"/>
                                        <Run Text="{Binding Task.SentDate, StringFormat='dd.MM.yyyy HH:mm'}" FontWeight="SemiBold"/>
                                    </TextBlock>
                                    
                                    <TextBlock FontSize="14" 
                                               Foreground="{DynamicResource SecondaryText}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,15">
                                        <Run Text="Срок исполнения:"/>
                                        <Run Text="{Binding Task.DueDate, StringFormat='dd.MM.yyyy HH:mm'}" FontWeight="SemiBold"/>
                                    </TextBlock>
                                    
                                    <TextBlock FontSize="14" 
                                               Foreground="{DynamicResource SecondaryText}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,15">
                                        <Run Text="Текущий статус:"/>
                                        <Run Text="{Binding StatusText, Mode=OneWay}" FontWeight="SemiBold"/>
                                    </TextBlock>
                                    
                                    <Border Background="{DynamicResource ButtonSuccess}"
                                            CornerRadius="5"
                                            Padding="10,5"
                                            HorizontalAlignment="Left">
                                        <Border.Style>
                                            <Style TargetType="Border">
                                                <Setter Property="Visibility" Value="Collapsed"/>
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Task.IsCompleted}" Value="True">
                                                        <Setter Property="Visibility" Value="Visible"/>
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Border.Style>
                                        <StackPanel>
                                            <TextBlock Text="✓ Задание исполнено"
                                                       FontSize="14"
                                                       FontWeight="SemiBold"
                                                       Foreground="White"
                                                       Margin="0,0,0,5"/>
                                            <TextBlock FontSize="13" 
                                                       Foreground="White">
                                                <Run Text="Дата завершения:"/>
                                                <Run Text="{Binding Task.CompletedDate, StringFormat='dd.MM.yyyy HH:mm'}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Border>
                                </StackPanel>
                            </Border>
                        </StackPanel>
                    </ScrollViewer>
                </TabItem>
            </TabControl>
        </ScrollViewer>
        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Bottom">
            <Button Content="✅"
                    Command="{Binding MarkAsCompletedCommand}"
                    Background="{DynamicResource ButtonSuccess}"
                    Foreground="White"
                    FontSize="18"
                    Width="40"
                    Height="40"
                    BorderThickness="0"
                    Cursor="Hand"
                    Margin="0,0,10,0"
                    ToolTip="Отметить исполненным">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="20"
                                            Width="{TemplateBinding Width}"
                                            Height="{TemplateBinding Height}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{DynamicResource ButtonSuccessHover}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            
            <Button Content="✏️"
                    Command="{Binding EditCommand}"
                    Background="{DynamicResource ButtonPrimary}"
                    Foreground="White"
                    FontSize="18"
                    Width="40"
                    Height="40"
                    BorderThickness="0"
                    Cursor="Hand"
                    ToolTip="Редактировать задание">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}"
                                            CornerRadius="20"
                                            Width="{TemplateBinding Width}"
                                            Height="{TemplateBinding Height}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1976D2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <StackPanel Grid.Row="2"
                    HorizontalAlignment="Center"
                    Margin="0,25,0,0">
            <Button Padding="30,10"
                    FontSize="14"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="CloseButton_Click">
            <TextBlock Text="Закрыть"/>
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Background" Value="{DynamicResource ButtonPrimary}"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="5"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#1976D2"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
        </StackPanel>
    </Grid>
</Window>
