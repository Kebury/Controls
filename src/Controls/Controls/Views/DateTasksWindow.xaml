<Window x:Class="Controls.Views.DateTasksWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        Title="Задачи на дату" 
        Height="600" Width="800"
        MinHeight="500" MinWidth="600"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource SecondaryBackground}"
        ResizeMode="CanResizeWithGrip"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="📅 Задачи на дату" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="{DynamicResource PrimaryText}"
                       Margin="0,0,0,10"/>
            
            <TextBlock Text="{Binding DateDisplay}" 
                       FontSize="18" 
                       Foreground="{DynamicResource SecondaryText}"/>
            
            <TextBlock Margin="0,5,0,0">
                <Run Text="Всего заданий: " FontSize="14" Foreground="{DynamicResource SecondaryText}"/>
                <Run Text="{Binding TaskCount, Mode=OneWay}" FontSize="14" FontWeight="SemiBold" Foreground="{DynamicResource ButtonDanger}"/>
            </TextBlock>
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Tasks}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{DynamicResource PrimaryBackground}"
                                BorderBrush="{DynamicResource SecondaryBorder}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Margin="0,0,0,15"
                                Padding="20"
                                Cursor="Hand"
                                MouseLeftButtonDown="Border_MouseLeftButtonDown">
                            <Border.Effect>

                                <DropShadowEffect Color="#40000000" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                            </Border.Effect>

                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="{DynamicResource SecondaryBackground}"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>

                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                                    <TextBlock Text="{Binding Title}" 
                                               FontSize="18" 
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource PrimaryText}"
                                               TextWrapping="Wrap"/>
                                    <TextBlock Margin="0,5,0,0">
                                        <Run Text="ID: " FontSize="11" Foreground="{DynamicResource SecondaryText}"/>
                                        <Run Text="{Binding Id, Mode=OneWay}" FontSize="11" FontWeight="Bold" Foreground="{DynamicResource ButtonPrimary}"/>
                                    </TextBlock>
                                </StackPanel>

                                <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
                                    <TextBlock Text="⏰ " FontSize="14"/>
                                    <TextBlock Text="Время: " 
                                               FontSize="14" 
                                               Foreground="{DynamicResource SecondaryText}"/>
                                    <TextBlock Text="{Binding DueDate, StringFormat='HH:mm'}" 
                                               FontSize="14" 
                                               FontWeight="SemiBold"
                                               Foreground="{DynamicResource PrimaryText}"/>
                                </StackPanel>

                                <StackPanel Grid.Row="2" Orientation="Horizontal">
                                    <Border Background="{DynamicResource ButtonPrimary}" 
                                            CornerRadius="3" 
                                            Padding="10,4"
                                            Margin="0,0,10,0">
                                        <TextBlock Text="{Binding TaskTypeDisplay}" 
                                                   Foreground="White" 
                                                   FontSize="12"
                                                   FontWeight="SemiBold"/>
                                    </Border>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,15,0">
                                        <TextBlock Text="⚡ " FontSize="12" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding ImportanceDisplay}" 
                                                   FontSize="12" 
                                                   Foreground="{DynamicResource ButtonDanger}"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" Margin="0,0,15,0">
                                        <TextBlock Text="🔥 " FontSize="12" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding UrgencyDisplay}" 
                                                   FontSize="12" 
                                                   Foreground="{DynamicResource AccentOrange}"
                                                   FontWeight="SemiBold"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="👤 " FontSize="12" VerticalAlignment="Center"/>
                                        <TextBlock Text="{Binding Assignee}" 
                                                   FontSize="12" 
                                                   Foreground="{DynamicResource SecondaryText}"
                                                   VerticalAlignment="Center"/>
                                    </StackPanel>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <Button Grid.Row="2" 
                Content="Закрыть"
                Command="{Binding CloseCommand}"
                Background="{DynamicResource ButtonSecondary}"
                Foreground="White"
                FontSize="14"
                FontWeight="SemiBold"
                Padding="30,12"
                BorderThickness="0"
                Cursor="Hand"
                HorizontalAlignment="Center"
                Margin="0,20,0,0">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        CornerRadius="5"
                                        Padding="{TemplateBinding Padding}">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="{DynamicResource ButtonSecondaryHover}"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
