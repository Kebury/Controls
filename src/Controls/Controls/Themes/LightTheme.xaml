<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:helpers="clr-namespace:Controls.Helpers">
    
    <SolidColorBrush x:Key="PrimaryBackground" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SecondaryBackground" Color="#ECF0F1"/>
    <SolidColorBrush x:Key="TertiaryBackground" Color="#F8F9FA"/>
    
    <SolidColorBrush x:Key="PrimaryText" Color="#2C3E50"/>
    <SolidColorBrush x:Key="SecondaryText" Color="#7F8C8D"/>
    <SolidColorBrush x:Key="TertiaryText" Color="#95A5A6"/>
    
    <SolidColorBrush x:Key="PrimaryBorder" Color="#BDC3C7"/>
    <SolidColorBrush x:Key="SecondaryBorder" Color="#ECF0F1"/>
    
    <SolidColorBrush x:Key="SidebarBackground" Color="#2C3E50"/>
    <SolidColorBrush x:Key="SidebarText" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="SidebarSecondaryText" Color="#BDC3C7"/>
    <SolidColorBrush x:Key="SidebarButtonHover" Color="#34495E"/>
    <SolidColorBrush x:Key="SidebarSeparator" Color="#34495E"/>
    
    <SolidColorBrush x:Key="ButtonPrimary" Color="#3498DB"/>
    <SolidColorBrush x:Key="ButtonPrimaryHover" Color="#2980B9"/>
    <SolidColorBrush x:Key="ButtonSuccess" Color="#27AE60"/>
    <SolidColorBrush x:Key="ButtonSuccessHover" Color="#229954"/>
    <SolidColorBrush x:Key="ButtonDanger" Color="#E74C3C"/>
    <SolidColorBrush x:Key="ButtonDangerHover" Color="#C0392B"/>
    <SolidColorBrush x:Key="ButtonSecondary" Color="#95A5A6"/>
    <SolidColorBrush x:Key="ButtonSecondaryHover" Color="#7F8C8D"/>
    <SolidColorBrush x:Key="ButtonWarning" Color="#F39C12"/>
    <SolidColorBrush x:Key="ButtonWarningHover" Color="#E67E22"/>
    
    <SolidColorBrush x:Key="DataGridBackground" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="DataGridAlternatingRow" Color="#F8F9FA"/>
    <SolidColorBrush x:Key="DataGridHeaderBackground" Color="#ECF0F1"/>
    <SolidColorBrush x:Key="DataGridHeaderText" Color="#2C3E50"/>
    <SolidColorBrush x:Key="DataGridBorder" Color="#BDC3C7"/>
    <SolidColorBrush x:Key="DataGridSelection" Color="#D6EAF8"/>
    
    <SolidColorBrush x:Key="InputBackground" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="InputBorder" Color="#BDC3C7"/>
    <SolidColorBrush x:Key="InputBorderFocus" Color="#3498DB"/>
    <SolidColorBrush x:Key="InputText" Color="#2C3E50"/>
    
    <SolidColorBrush x:Key="CalendarTodayBackground" Color="#3498DB"/>
    <SolidColorBrush x:Key="CalendarTodayText" Color="#FFFFFF"/>
    <SolidColorBrush x:Key="CalendarSelectedBackground" Color="#E8F4FD"/>
    <SolidColorBrush x:Key="CalendarHoverBackground" Color="#F0F0F0"/>
    
    <SolidColorBrush x:Key="AccentBlue" Color="#3498DB"/>
    <SolidColorBrush x:Key="AccentGreen" Color="#27AE60"/>
    <SolidColorBrush x:Key="AccentRed" Color="#E74C3C"/>
    <SolidColorBrush x:Key="AccentOrange" Color="#F39C12"/>
    <SolidColorBrush x:Key="AccentPurple" Color="#9B59B6"/>
    <SolidColorBrush x:Key="AccentAmber" Color="#FFA500"/>
    
    <SolidColorBrush x:Key="BadgeBackground" Color="#E74C3C"/>
    <SolidColorBrush x:Key="BadgeText" Color="#FFFFFF"/>
    
    <SolidColorBrush x:Key="CalendarOverdueBackground" Color="#FFE0E0"/>
    <SolidColorBrush x:Key="CalendarOverdueBorder" Color="#E74C3C"/>
    
    <SolidColorBrush x:Key="ButtonDangerLight" Color="#FADBD8"/>
    <SolidColorBrush x:Key="ButtonWarningDark" Color="#D68910"/>
    <SolidColorBrush x:Key="SuccessBackground" Color="#D5F4E6"/>
    <SolidColorBrush x:Key="WarningBackground" Color="#FCF3CF"/>
    <SolidColorBrush x:Key="DataGridHeader" Color="#ECF0F1"/>
    <SolidColorBrush x:Key="DataGridSelectedRow" Color="#D6EAF8"/>
    
    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton"
                                    Grid.Column="2"
                                    Focusable="false"
                                    IsChecked="{Binding Path=IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                    ClickMode="Press"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border x:Name="Border"
                                          Background="{TemplateBinding Background}"
                                          BorderBrush="{TemplateBinding BorderBrush}"
                                          BorderThickness="{TemplateBinding BorderThickness}"
                                          CornerRadius="4">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="20"/>
                                            </Grid.ColumnDefinitions>
                                            <Path x:Name="Arrow"
                                                Grid.Column="1"
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                Data="M 0 0 L 4 4 L 8 0 Z"
                                                Fill="{Binding Foreground, RelativeSource={RelativeSource AncestorType=ComboBox}}"/>
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite"
                                        IsHitTestVisible="False"
                                        Content="{TemplateBinding SelectionBoxItem}"
                                        ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                        ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                        Margin="8,3,23,3"
                                        VerticalAlignment="Center"
                                        HorizontalAlignment="Left"/>
                        <TextBox x:Name="PART_EditableTextBox"
                               Style="{x:Null}"
                               HorizontalAlignment="Left"
                               VerticalAlignment="Center"
                               Margin="3,3,23,3"
                               Focusable="True"
                               Background="Transparent"
                               Foreground="{TemplateBinding Foreground}"
                               Visibility="Hidden"
                               IsReadOnly="{TemplateBinding IsReadOnly}"/>
                        <Popup x:Name="Popup"
                             Placement="Bottom"
                             IsOpen="{TemplateBinding IsDropDownOpen}"
                             AllowsTransparency="True"
                             Focusable="False"
                             PopupAnimation="Slide">
                            <Grid x:Name="DropDown"
                                SnapsToDevicePixels="True"
                                MinWidth="{TemplateBinding ActualWidth}"
                                MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                <Border x:Name="DropDownBorder"
                                      Background="{DynamicResource InputBackground}"
                                      BorderThickness="1"
                                      BorderBrush="{DynamicResource InputBorder}"
                                      CornerRadius="4"
                                      Margin="0,1,0,0">
                                    <ScrollViewer Margin="2"
                                                SnapsToDevicePixels="True">
                                        <StackPanel IsItemsHost="True"
                                                  KeyboardNavigation.DirectionalNavigation="Contained"/>
                                    </ScrollViewer>
                                </Border>
                            </Grid>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="HasItems" Value="false">
                            <Setter TargetName="DropDownBorder" Property="MinHeight" Value="95"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="false">
                            <Setter Property="Foreground" Value="{DynamicResource SecondaryText}"/>
                        </Trigger>
                        <Trigger Property="IsGrouping" Value="true">
                            <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                        </Trigger>
                        <Trigger Property="IsEditable" Value="true">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible"/>
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
    
    <Style TargetType="ComboBoxItem">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{DynamicResource TertiaryBackground}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{DynamicResource DataGridSelection}"/>
            </Trigger>
        </Style.Triggers>
    </Style>
    
    <Style TargetType="DatePicker">
        <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="5"/>
    </Style>
    
    <Style TargetType="DatePickerTextBox">
        <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource InputText}"/>
        <Setter Property="BorderThickness" Value="0"/>
    </Style>
    
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource InputBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource InputText}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource InputBorder}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="8,5"/>
        <Setter Property="CaretBrush" Value="{DynamicResource PrimaryText}"/>
    </Style>
    
    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="{DynamicResource DataGridHeaderBackground}"/>
        <Setter Property="Foreground" Value="{DynamicResource DataGridHeaderText}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryBorder}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>
    
    <Style TargetType="ScrollViewer">
        <Setter Property="helpers:ScrollBehavior.EnableMouseWheelScroll" Value="True"/>
        <Setter Property="Focusable" Value="True"/>
    </Style>
    
</ResourceDictionary>
